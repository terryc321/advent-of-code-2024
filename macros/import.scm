

(import (chicken format))
(import (chicken pretty-print))
(import (srfi-1))
(import (simple-loops))
(define fmt format)
(import procedural-macros)
(import (chicken bitwise))
(import expand-full)

(import (chicken io))

(import (chicken format))
(import (chicken pretty-print))
(import (srfi-1))

(import (simple-loops)) ;; ?? really bad macro loops 

(define fmt format)
(import procedural-macros) ;; define-macro 
(import (srfi-69)) ;; hash table

(import (chicken bitwise)) 
(import (chicken sort))



